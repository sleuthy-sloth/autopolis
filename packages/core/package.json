{
  "name": "@autopolis/core",
  "version": "0.1.0",
  "private": true,
  "description": "Autopolis deterministic simulation core — spatial grid model, terrain generation, snapshots. Pure TS, zero dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
